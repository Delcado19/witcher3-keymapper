{
  "name": "witcher3-keymapper",
  "version": "0.1.0",
  "private": true,
  "description": "Local Witcher 3 input.settings mapper and conflict scanner.",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=20"
  }
}
