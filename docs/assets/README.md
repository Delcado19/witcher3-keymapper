# Assets

Place the GitHub social preview banner here as:

```text
witcher3-keymapper-social-preview.png
```

Recommended size: 1280 x 640 px. The app styling in `public/styles.css` is
matched to the dark metal, gold linework and muted red accent direction of that
banner.
